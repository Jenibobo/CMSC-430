function main param1: integer, param2: integer returns boolean;
    var1: integer is 100;
    var2: integer is 10;
    var_3: boolean is true;

    begin
        if (param1 > var1 or not param2 < var2)
            then
                reduce +
                   10 * 10 + 2;
                   100 rem 10;
                   10 * 10 ** 2;
                   10 - 2;
                   10 / 5;
                endreduce;
        else
            case var1 is
                when 0 => var1 - 30;
                when 1 => var1 + 30;
                when 2 => var1 * 30;
                others => var1 = 0;
            endcase;
        endif;
    end;
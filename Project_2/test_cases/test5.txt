function main param1: integer, param2: real returns boolean;
    var1: integer is 100;
    var2: real is 12.0;
    var_3: boolean is true;

begin
    if param1 > var1 or not param1 > var1 then
        reduce 
            * 2;
        endreduce;
    else
        case param1 is
            when 0 => param1 = 2;
            when 1 => param1 /= 4;
            when 2 => param1 >= 6;
            others => param1 <= 8;
        endcase;
    endif;
    
end;
// Function testing case testing all lexemes.

function test_3 returns boolean;
  s: real is 3.3;

begin
  if s then

    case s is
      when 0 => s >= 0;
      when 1 => s <= 1;
      others => s /= 3;
    endcase;

  else

    if s >= 3 and not x < 0 then
      true;
    else
      false;
    endif;

  endif;
end;
